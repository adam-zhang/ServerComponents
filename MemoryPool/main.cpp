#include <iostream>
#include <memory>
#include <vector>
#include <list>
#include "MemoryPool.h"

using namespace std;

int main(int argc, char** argv)
{
	//ServerComponents::MemoryPool<int> pool;

	//for(int i = 0; i != 100; ++i)
	//	std::shared_ptr<int> p = std::shared_ptr<int>(pool.alloc()); // &ServerComponents::MemoryPool::dealloc);
	return 0;
}
